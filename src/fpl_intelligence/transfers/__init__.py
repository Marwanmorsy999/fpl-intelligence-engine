"""Phase 25 Gate 0 (T1) — TRANSFER INTELLIGENCE.

Official source first: ``/api/entry/{entry_id}/history/`` through the egress
masks yields the per-transfer ledger (``transfers_log``). When every mask
fails, consecutive squad snapshots are diffed instead and rows are labelled
``snapshot-diff (unofficial)``. A failed history fetch is surfaced as an
honest "unavailable" chip — never fabricated rows.
"""
