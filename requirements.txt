# Runtime dependencies for the FPL Intelligence Engine API.
# The canonical source of truth is pyproject.toml; Vercel builds run `pip install .`
# which installs the package and these pinned deps. This file is kept in sync for
# Docker / local installs.

# --- web framework / api ---
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
python-multipart>=0.0.9

# --- config / settings ---
pydantic>=2.10,<3
pydantic-settings>=2.7,<3
python-dotenv>=1.0

# --- data / db ---
sqlalchemy>=2.0,<3
psycopg[binary]>=3.2,<4
alembic>=1.15,<2

# --- resilience / misc ---
tenacity>=9,<10
pyyaml>=6.0,<7
typer>=0.15,<1
rich>=13.9,<14

# --- numerics (models) ---
numpy>=1.26,<3
scipy>=1.11,<2

# --- telegram bot (Phase 10.2) ---
python-telegram-bot>=21.0,<22

# --- self-hosted web push (Phase 23) ---
pywebpush>=2.0,<3

# --- error tracking (Phase 4.4, free 5k errors/mo) ---
sentry-sdk>=2.20,<3

# --- auth / server (prod) ---
requests>=2.31
